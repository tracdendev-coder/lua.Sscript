-- =====================================================
-- ESP HACK v4.1 – KHUNG HÌNH VUÔNG HITBOX ĐỘNG, MENU KÉO THẢ CHUẨN
-- =====================================================
-- ĐẶT TRONG: StarterGui / StarterPlayerScripts (LocalScript)
-- =====================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera

local LocalPlayer = Players.LocalPlayer
local gui = Instance.new("ScreenGui")
gui.Name = "ESP_Menu_Hack"
gui.ResetOnSpawn = false
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- ========== CẤU HÌNH ==========
local ESP_ENABLED = true
local SIZE_MULTIPLIER = 1.0
local MIN_MULTIPLIER = 0.3
local MAX_MULTIPLIER = 2.5
local MENU_MINIMIZED = false

-- ========== BIẾN TOÀN CỤC ==========
local espFrames = {}
local activePlayers = {}

-- ========== TẠO MENU CHÍNH ==========
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 260, 0, 180)
mainFrame.Position = UDim2.new(0, 50, 0, 50)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
mainFrame.BackgroundTransparency = 0.2
mainFrame.BorderSizePixel = 1
mainFrame.BorderColor3 = Color3.fromRGB(0, 255, 200)
mainFrame.ClipsDescendants = true
mainFrame.Parent = gui

-- Thanh tiêu đề (kéo thả)
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 26)
titleBar.BackgroundColor3 = Color3.fromRGB(0, 100, 180)
titleBar.BackgroundTransparency = 0.4
titleBar.Parent = mainFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -30, 1, 0)
titleLabel.Position = UDim2.new(0, 5, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "ESP HACK v4.1"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 14
titleLabel.Parent = titleBar

-- Nút thu nhỏ
local toggleMenuBtn = Instance.new("TextButton")
toggleMenuBtn.Size = UDim2.new(0, 22, 0, 22)
toggleMenuBtn.Position = UDim2.new(1, -26, 0, 2)
toggleMenuBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
toggleMenuBtn.Text = "-"
toggleMenuBtn.TextColor3 = Color3.fromRGB(255,255,255)
toggleMenuBtn.Font = Enum.Font.GothamBold
toggleMenuBtn.TextSize = 18
toggleMenuBtn.Parent = titleBar

-- Nút bật/tắt ESP
local toggleEspBtn = Instance.new("TextButton")
toggleEspBtn.Size = UDim2.new(0, 120, 0, 32)
toggleEspBtn.Position = UDim2.new(0.5, -60, 0, 40)
toggleEspBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
toggleEspBtn.Text = "TẮT ESP"
toggleEspBtn.TextColor3 = Color3.fromRGB(255,255,255)
toggleEspBtn.Font = Enum.Font.GothamBold
toggleEspBtn.TextSize = 14
toggleEspBtn.Parent = mainFrame

-- Hệ số
local sizeLabel = Instance.new("TextLabel")
sizeLabel.Size = UDim2.new(0, 90, 0, 22)
sizeLabel.Position = UDim2.new(0, 10, 0, 85)
sizeLabel.BackgroundTransparency = 1
sizeLabel.Text = "Hệ số:"
sizeLabel.TextColor3 = Color3.fromRGB(200,200,200)
sizeLabel.TextSize = 13
sizeLabel.Font = Enum.Font.Gotham
sizeLabel.TextXAlignment = Enum.TextXAlignment.Left
sizeLabel.Parent = mainFrame

local sizeInput = Instance.new("TextBox")
sizeInput.Size = UDim2.new(0, 60, 0, 22)
sizeInput.Position = UDim2.new(0, 100, 0, 85)
sizeInput.BackgroundColor3 = Color3.fromRGB(40,40,55)
sizeInput.Text = string.format("%.1f", SIZE_MULTIPLIER)
sizeInput.TextColor3 = Color3.fromRGB(255,255,255)
sizeInput.Font = Enum.Font.Gotham
sizeInput.TextSize = 13
sizeInput.Parent = mainFrame

local resetPosBtn = Instance.new("TextButton")
resetPosBtn.Size = UDim2.new(0, 90, 0, 26)
resetPosBtn.Position = UDim2.new(0.5, -45, 0, 130)
resetPosBtn.BackgroundColor3 = Color3.fromRGB(70,70,110)
resetPosBtn.Text = "Reset vị trí"
resetPosBtn.TextColor3 = Color3.fromRGB(255,255,255)
resetPosBtn.Font = Enum.Font.Gotham
resetPosBtn.TextSize = 12
resetPosBtn.Parent = mainFrame

-- ========== KÉO THẢ MENU (dùng UserInputService) ==========
local dragData = {
    isDragging = false,
    startMousePos = nil,
    startFramePos = nil,
    connection = nil
}

local function startDrag(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        -- Kiểm tra xem con trỏ có nằm trên titleBar không
        local mousePos = input.Position
        local absPos = titleBar.AbsolutePosition
        local absSize = titleBar.AbsoluteSize
        if mousePos.X >= absPos.X and mousePos.X <= absPos.X + absSize.X and
           mousePos.Y >= absPos.Y and mousePos.Y <= absPos.Y + absSize.Y then
            dragData.isDragging = true
            dragData.startMousePos = input.Position
            dragData.startFramePos = mainFrame.Position
        end
    end
end

local function updateDrag(input)
    if dragData.isDragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragData.startMousePos
        mainFrame.Position = UDim2.new(
            dragData.startFramePos.X.Scale,
            dragData.startFramePos.X.Offset + delta.X,
            dragData.startFramePos.Y.Scale,
            dragData.startFramePos.Y.Offset + delta.Y
        )
    end
end

local function endDrag(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragData.isDragging = false
    end
end

UserInputService.InputBegan:Connect(startDrag)
UserInputService.InputChanged:Connect(updateDrag)
UserInputService.InputEnded:Connect(endDrag)

-- ========== THU NHỎ / MỞ RỘNG ==========
local function updateMenuSize(minimized)
    MENU_MINIMIZED = minimized
    if minimized then
        mainFrame.Size = UDim2.new(0, 260, 0, 66)
        toggleMenuBtn.Text = "+"
        sizeLabel.Visible = false
        sizeInput.Visible = false
        resetPosBtn.Visible = false
        toggleEspBtn.Position = UDim2.new(0.5, -60, 0, 30)
    else
        mainFrame.Size = UDim2.new(0, 260, 0, 180)
        toggleMenuBtn.Text = "-"
        sizeLabel.Visible = true
        sizeInput.Visible = true
        resetPosBtn.Visible = true
        toggleEspBtn.Position = UDim2.new(0.5, -60, 0, 40)
    end
end

toggleMenuBtn.MouseButton1Click:Connect(function()
    updateMenuSize(not MENU_MINIMIZED)
end)

-- ========== BẬT/TẮT ESP ==========
local function clearAllFrames()
    for _, frame in pairs(espFrames) do
        if frame and frame.Parent then frame:Destroy() end
    end
    espFrames = {}
    activePlayers = {}
end

local function rebuildAllFrames()
    clearAllFrames()
    if not ESP_ENABLED then return end
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local frame = Instance.new("Frame")
            frame.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
            frame.BackgroundTransparency = 0.6
            frame.BorderSizePixel = 2
            frame.BorderColor3 = Color3.fromRGB(0, 255, 0)
            frame.Visible = true
            frame.ZIndex = 10
            frame.Parent = gui
            espFrames[player] = frame
            activePlayers[player] = true
        end
    end
end

toggleEspBtn.MouseButton1Click:Connect(function()
    ESP_ENABLED = not ESP_ENABLED
    toggleEspBtn.Text = ESP_ENABLED and "TẮT ESP" or "BẬT ESP"
    toggleEspBtn.BackgroundColor3 = ESP_ENABLED and Color3.fromRGB(0,180,80) or Color3.fromRGB(180,50,50)
    if ESP_ENABLED then
        rebuildAllFrames()
    else
        clearAllFrames()
    end
end)

-- ========== HỆ SỐ KÍCH THƯỚC ==========
local function applySizeMultiplier(value)
    local num = tonumber(value)
    if num then
        SIZE_MULTIPLIER = math.clamp(num, MIN_MULTIPLIER, MAX_MULTIPLIER)
        sizeInput.Text = string.format("%.1f", SIZE_MULTIPLIER)
    else
        sizeInput.Text = string.format("%.1f", SIZE_MULTIPLIER)
    end
end

sizeInput.FocusLost:Connect(function(enter)
    if enter then applySizeMultiplier(sizeInput.Text) end
end)

resetPosBtn.MouseButton1Click:Connect(function()
    mainFrame.Position = UDim2.new(0, 50, 0, 50)
end)

-- ========== CẬP NHẬT KHUNG ESP ==========
local function updateEspFrames()
    if not ESP_ENABLED then return end
    for player, frame in pairs(espFrames) do
        if player == LocalPlayer then continue end
        local char = player.Character
        if not char then frame.Visible = false continue end
        local head = char:FindFirstChild("Head")
        local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
        local root = char:FindFirstChild("HumanoidRootPart")
        if not head or not torso or not root then frame.Visible = false continue end

        local headPos = head.Position
        local footPos = torso.Position - Vector3.new(0, 2.5, 0)
        local screenHead, onHead = Camera:WorldToScreenPoint(headPos)
        local screenFoot, onFoot = Camera:WorldToScreenPoint(footPos)

        if onHead and onFoot and screenHead.Z > 0 and screenFoot.Z > 0 then
            local height = math.abs(screenHead.Y - screenFoot.Y)
            local side = math.clamp(height * SIZE_MULTIPLIER, 10, 600)
            local screenRoot, onRoot = Camera:WorldToScreenPoint(root.Position)
            if not onRoot or screenRoot.Z <= 0 then frame.Visible = false continue end

            frame.Size = UDim2.new(0, side, 0, side)
            frame.Position = UDim2.new(0, screenRoot.X - side/2, 0, screenRoot.Y - side/2)
            frame.Visible = true
        else
            frame.Visible = false
        end
    end
end

-- ========== QUẢN LÝ NGƯỜI CHƠI ==========
local function addPlayer(player)
    if player == LocalPlayer or espFrames[player] then return end
    local frame = Instance.new("Frame")
    frame.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    frame.BackgroundTransparency = 0.6
    frame.BorderSizePixel = 2
    frame.BorderColor3 = Color3.fromRGB(0, 255, 0)
    frame.Visible = true
    frame.ZIndex = 10
    frame.Parent = gui
    espFrames[player] = frame
    activePlayers[player] = true
end

local function removePlayer(player)
    local frame = espFrames[player]
    if frame then frame:Destroy() end
    espFrames[player] = nil
    activePlayers[player] = nil
end

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function()
        if ESP_ENABLED and player ~= LocalPlayer then addPlayer(player) end
    end)
    if player.Character then addPlayer(player) end
end)

Players.PlayerRemoving:Connect(removePlayer)

-- Khởi tạo ban đầu
for _, player in pairs(Players:GetPlayers()) do
    if player ~= LocalPlayer then
        player.CharacterAdded:Connect(function()
            if ESP_ENABLED then addPlayer(player) end
        end)
        if player.Character then addPlayer(player) end
    end
end

-- ========== VÒNG LẶP RENDER ==========
RunService.RenderStepped:Connect(updateEspFrames)

-- ========== DỌN DẸP ==========
gui.AncestryChanged:Connect(function()
    if not gui.Parent then clearAllFrames() end
end)

print("ESP HACK v4.1 – Kéo thả menu bằng cách giữ chuột trên thanh tiêu đề.")
